{
  "name": "letterboxd-elo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc -b && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "scrape": "bun run scripts/scrape-letterboxd.ts"
  },
  "dependencies": {
    "vite": "^7.2.7"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
